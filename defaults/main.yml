---
# defaults variable file
controller_user: ansible

install_type: archive
install_dir: /opt/ansible

package_name: ansible

archive_url: https://releases.ansible.com/ansible/ansible-2.9.2.tar.gz
archive_checksum: https://releases.ansible.com/ansible/ansible-2.9.2.tar.gz.sha
checksum_format: sha256

default_config_dir: /etc/ansible
ansible_config_file: ansible.cfg
config: {}

roles: []
collections: []

perform_uninstall: false
